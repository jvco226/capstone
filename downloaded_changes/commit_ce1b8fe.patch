commit ce1b8fe2222a8197537283c4f07cff0885fee747
Author: jvco226 <jvco226@uky.edu>
Date:   Tue Dec 9 12:52:54 2025 -0500

    Add files via upload

diff --git a/index.html b/index.html
index f2ca051..816eb9b 100644
--- a/index.html
+++ b/index.html
@@ -147,18 +147,16 @@
             <!-- Lobby Page -->
             <div id="lobby-page" class="page-content hidden">
                 <h1 class="text-4xl font-black mb-6 text-center text-red-600">Lobby</h1>
-                
                 <div class="text-center p-6 bg-gray-50 rounded-lg">
-                    <h2 class="text-2xl font-bold text-gray-700">Share this PIN with your friends.</h2>
-                    
-                    <!-- Display PIN -->
-                    <div class="my-4">
-                        <span id="lobby-game-pin" class="font-black text-red-600">...</span>
+                    <!-- Share PIN container -->
+                    <div id="share-pin-container" class="my-4">
+                        <h2 class="text-2xl font-bold text-gray-700">Share this PIN with your friends.</h2>
+                        <div class="my-4">
+                            <span id="lobby-game-pin" class="font-black text-red-600">...</span>
+                        </div>
                     </div>
-
                     <p class="text-lg text-gray-600 mb-6">Waiting for players to join...</p>
-
-                    <!-- PLACEHOLDER player list -->
+                    <!-- Player list -->
                     <div id="player-list" class="min-h-[100px] bg-white border border-gray-200 rounded-lg p-3 text-left mb-6">
                         <h3 class="font-bold text-gray-800 text-lg mb-2">Players (1/8)</h3>
                         <ul id="players" class="list-disc list-inside">
@@ -166,20 +164,11 @@
                         </ul>
                     </div>
                     <div class="grid grid-cols-2 gap-4">
-                        <button id="leave-lobby-btn"
-                                class="w-full flex justify-center py-3 px-4 border border-transparent rounded-lg shadow-sm text-lg font-bold uppercase tracking-wider text-white bg-red-500 hover:bg-red-600 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-400">
-                            Leave Lobby
-                        </button>
-                        <button id="start-game-btn"
-                                class="w-full flex justify-center py-3 px-4 border border-transparent rounded-lg shadow-sm text-lg font-bold uppercase tracking-wider text-white bg-green-600 hover:bg-green-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-green-400">
-                            Start Game!
-                        </button>
+                        <button id="leave-lobby-btn" class="w-full flex justify-center py-3 px-4 border border-transparent rounded-lg shadow-sm text-lg font-bold uppercase tracking-wider text-white bg-red-500 hover:bg-red-600 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-400"> Leave Lobby </button>
+                        <button id="start-game-btn" class="w-full flex justify-center py-3 px-4 border border-transparent rounded-lg shadow-sm text-lg font-bold uppercase tracking-wider text-white bg-green-600 hover:bg-green-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-green-400"> Start Game! </button>
                     </div>
                 </div>
             </div>
-
-
-            </div>
         </div>
     </div>
 
@@ -401,15 +390,28 @@
         });
 
         // Listen for server confirmation
-        socket.on('joinSuccess', ({ code, players: serverPlayers }) => {
+        socket.on('joinSuccess', ({ code, players: serverPlayers, isPublic }) => {
             currentRoomCode = code;
-            lobbyGamePin.textContent = code;
             players = serverPlayers.map(p => p.name);
             updatePlayerList();
             showPage('lobby-page');
             showMessage('Room joined!', 'success');
+
+            const sharePinContainer = document.getElementById('share-pin-container');
+
+            if (isPublic) {
+                // Hide PIN and share message
+                lobbyGamePin.textContent = 'Public Game';
+                sharePinContainer.style.display = 'none';
+            } else {
+                // Show PIN and message
+                lobbyGamePin.textContent = code;
+                sharePinContainer.style.display = 'block';
+            }
         });
 
+
+
         // Listen for join errors
         socket.on('joinError', (message) => {
             showMessage(message, 'error');
diff --git a/server.js b/server.js
index 16dfe09..cd5baf8 100644
--- a/server.js
+++ b/server.js
@@ -105,7 +105,12 @@ io.on('connection', (socket) => {
         io.to(code).emit('updatePlayers', rooms[code].players);
 
         // Confirm join to client
-        socket.emit('joinSuccess', { code, players: rooms[code].players });
+        socket.emit('joinSuccess', { 
+            code, 
+            players: rooms[code].players, 
+            isPublic: rooms[code].isPublic || false 
+        });
+
     });
 
     socket.on('leaveRoom', (code) => {
